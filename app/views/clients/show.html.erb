<p style="color: green"><%= notice %></p>

<h1>Información Cliente</h1>
<%= render @client %>

<div>
  <div class="container">
    <div class="row">
      <div class="col-10">
        <%= link_to "Editar Cliente", edit_client_path(@client), class:"btn btn-success mt-2"%>
        <%= link_to "Volver a Clientes", clients_path, class:"btn btn-primary mt-2" %>
      </div>
    </div>
  </div>
  
  <div class="container">
    <%= button_to "Eliminar Cliente",@client, method: :delete, class:"btn btn-danger mt-2 mb-5"%>  
  </div>
</div>
